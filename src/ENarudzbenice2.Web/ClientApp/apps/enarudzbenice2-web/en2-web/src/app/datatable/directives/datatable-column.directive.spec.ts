import { DatatableColumnDirective } from './datatable-column.directive';

describe('DatatableColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new DatatableColumnDirective();
    expect(directive).toBeTruthy();
  });
});
