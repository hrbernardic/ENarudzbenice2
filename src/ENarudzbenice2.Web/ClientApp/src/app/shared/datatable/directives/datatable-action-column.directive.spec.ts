import { DatatableActionColumnDirective } from './datatable-action-column.directive';

describe('DatatableActionColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new DatatableActionColumnDirective();
    expect(directive).toBeTruthy();
  });
});
